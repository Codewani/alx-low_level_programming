#include <stdio.h>
#include "main.h"
/**
 *add - Tells you whether the parameter is lower case or not
 *@c: accepts integers c and d
 *@d: accepts integers c and d
 *Return: the sum of and c and d
 */
int add(int c, int d)
{
	return (c + d);
}
